[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "financial_robot"
version = "0.1.0"
description = "智能财务机器人 Lec01 — Intelligent Financial Robot"
requires-python = ">=3.8"

[project.scripts]
financial-robot = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
